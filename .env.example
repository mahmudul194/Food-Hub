# --- Database Configuration ---
# Use separate values:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=foodhub

# OR use a single connection string (standard for many free hosts):
# DATABASE_URL=mysql://user:pass@host:port/dbname

# --- Server Configuration ---
API_PORT=5000
NODE_ENV=production
